# Archived QuickFS Pipeline

The original QuickFS-oriented Buffett workflow remains in `model/` as the legacy reference implementation described by `docs/legacy-buffett-model.md`.

This archive directory exists to make the modernization boundary explicit and to hold any future frozen copies of the legacy scripts if they are moved out of `model/`.
